# Variables públicas (cliente)
NEXT_PUBLIC_ROBOT_ID=robot-001
# Recomendado en Docker+Nginx: dejar vacío para same-origin (Nginx proxya /socket.io)
# En desarrollo local sin Nginx, puedes usar: http://localhost:3001
NEXT_PUBLIC_SOCKET_URL=

# Seguridad (servidor)
# IMPORTANTE: pon un secreto largo y único en producción
# Ejemplo (NO usar en producción):
AUTH_SECRET=change-me

# SQLite
# Si no se define, usa: data/robot.sqlite (dentro del proyecto)
# En producción conviene apuntar a una ruta persistente fuera del release (VPS/Docker)
SQLITE_PATH=data/robot.sqlite
# En Docker (con volumen en /app/data) recomendado:
# SQLITE_PATH=/app/data/robot.sqlite

# Seed de admin (solo si la DB está vacía o no existe el usuario)
DEFAULT_ADMIN_USER=admin
# Cámbiala en producción y, si quieres máxima seguridad, elimina esta variable después del primer arranque.
DEFAULT_ADMIN_PASS=admin123
